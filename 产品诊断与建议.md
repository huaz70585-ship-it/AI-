# 途灵 · 产品诊断与建议

> 盘点时间：2026-09-24 · 基于当前代码（`trval-h5` 前端 / `backend` 后端）与真库只读快照
> 所有结论都带证据（文件:行号 或 数据库统计），没有证据的部分会明确标注「推断」。

---

## 一句话结论

**这个项目真正的产品是「一句话生成可编辑行程的 AI 行前规划工具」，而且这一块已经跑通了；
但它的界面同时在扮演 OTA 商城和内容社区，而后两者只有壳、没有里子 —— 这是当前最大的认知错位。**

---

## 一、产品定位

### 1.1 它自己声称的定位

`trval-h5/README.md:3` 写的是：

> H5 端 AI 智能旅游平台 —— 工具效率型打底，叠加轻 AI 规划。主打「效率、简洁、AI」三件事。

品牌名「途灵」，Slogan 落在首页 AI Banner 上：**「告诉 AI 你的假期，8 秒生成可编辑行程」**（`Home.vue:22`）。

### 1.2 它实际做成了什么

真正跑通、且别家替代成本高的链路只有一条：

```
一句话需求 → DeepSeek 生成结构化 JSON 行程 → 落库成「天 + 项 + 人均价」
→ 时间轴展示 → 用户可改日期/人数/移动/删除 → 相邻项自动算交通衔接
```

支撑这条链路的真实能力（都有代码与实测证据）：

| 能力 | 位置 | 状态 |
|---|---|---|
| 一句话生成整条行程 | `backend/src/routes/chat.js:27-34` | 真 |
| SSE 流式输出 | `chat.js:41-127` | 真 |
| 结构化落库（天/项/价） | `backend/src/routes/trip.js:161` | 真 |
| 改日期 → 天数联动 + AI 补排新天 | `trip.js:379` | 真（9-24 上线） |
| 天数变化 → 摘掉失效的返程/接机项 | `utils/tripBoundary.js` | 真 |
| 相邻行程项 → 交通方式/时长/费用 | `backend/src/routes/hop.js:173` | 真（带缓存） |
| 每天天气 + 恶劣天气提醒 | `routes/weather.js:138`、`utils/weatherAlert.ts` | 真 |
| 预算随人数/项目自动重算 | `trip.js` 的 `recalcTripBudget()` | 真 |

**这条链路是产品的命根子，它没有第二个替代品。** 其余部分大多是装饰。

### 1.3 定位错位：它在同时扮演三种产品

| 扮演的角色 | 证据 | 真实度 |
|---|---|---|
| **AI 原生工具** | 首页 AI Banner、金刚区 5 个 AI 入口、对话页生成行程 | ✅ 真的 |
| **OTA 商城**（携程/飞猪） | 首页卡片「¥500 起 / 328 人报名」`Home.vue:119-124`；行程列表「X 项待预订 / 去预订」`TripList.vue:47-50`；消息中心「去支付」落点 `Message.vue:49`；我的页「我的订单」带红点 `profile.vue:86-92` | ❌ 全是壳 |
| **内容社区**（小红书） | 首页「为你推荐」内容流 `Home.vue:49-129`；对话页「引用 N 篇来源」`Chat.vue:39-48` | ❌ 全是壳 |

最刺眼的一处：**后端 24 条路由里没有任何一条与订单、支付、预订有关**，但界面在四个不同页面用四种方式告诉用户"这里可以预订"。

---

## 二、适用人群

### 2.1 现在真正能服务的人

- **行前的自由行游客**：目的地在北京 / 上海 / 成都 / 杭州（或有大众认知的城市），行程 1–5 天
- **要"能直接用的东西"而不是"一段漂亮文字"的人**：他们要的是按天排好的清单，这正是当前输出形态
- **不想花两小时做攻略的人**：核心价值是省时间，不是省钱
- **不需要平台代订的人**：习惯自己在携程/12306 下单

一句话画像（**推断**）：**25–35 岁、一二线城市、自由行、周末或小长假出行、
愿意为"省事"付费但不愿被平台赚差价的人。**

典型使用时刻：*周五晚上想起下周末要去某地，10 分钟内要一份能直接照着走的行程。*

### 2.2 现在服务不了的人（重要，这是需求边界）

| 人群 | 为什么服务不了 | 证据 |
|---|---|---|
| 要"一键下单"的人 | 无交易层 | 24 条路由无订单/支付 |
| 出境游用户 | Prompt 明确限定中国境内 | `chat.js:28`「专注中国境内旅行规划」 |
| 深度/长线旅行者 | 景点库只有 4 城 16 条，新疆这类目的地全靠模型自由发挥 | `cities` 4 行、`city_spots` 16 行 |
| 亲子/多家庭复杂出行 | 只有成人/儿童两档计价（儿童半价），无酒店、无票务 | `Trip.vue:199-218` |
| 想边玩边记录的人 | 出行中/出行后完全没有环节 | 见 3.4 |

### 2.3 「我的」页面在讲一个不成立的故事

`profile.vue:14-47` 展示「探索者 Lv.4」「已解锁 12 城 / 46 天 / 8,240 km」，
数据来自 `footprints` 表 —— 该表**只有 3 行种子数据**，且没有任何业务逻辑在写它。
这等于给用户一个**永远是假的**成就体系。

---

## 三、缺口清单（按优先级）

### 3.1 🔴 交易层的"承诺"与实现的落差

- UI 承诺：`TripList.vue:47-50`「去预订」、`Message.vue:49`「去支付」、`profile.vue:91`「我的订单」
- 实际：**零实现**。点「去预订」跳行程详情；「我的订单」没有 `@click`
- **影响**：这是全文最伤信任的一处。用户看到"待预订"会以为可以下单，点下去发现什么都没有。

### 3.2 🔴 一批"点不动"的死入口

| 位置 | 现象 |
|---|---|
| `Trip.vue:35-37` | 「地图」Tab 只有样式，**没有 `@click`**，切不过去 |
| `Trip.vue:151` | FAB「新增安排」→ `showToast('添加安排功能开发中')` |
| `Chat.vue:84-87` | 「复制 / 重试 / 编辑 / 反馈」四个按钮**全部无 `@click`** |
| `Chat.vue:39-48` | 「引用 N 篇来源」：`citation` 字段仅在类型里定义（`Chat.vue:154`），**全文件无任何赋值** → 该区块永不渲染 |
| `profile.vue:86-104` | 「我的订单 / 收藏 / 优惠券 / 相册」四个入口**全部无 `@click`** |
| `profile.vue:31` | 「足迹地图」按钮无 `@click` |
| `Home.vue:96-127` | 首页景点卡（带价格和报名数）**整张卡无 `@click`**，看完点不进去 |

README 里写的「对话六件套」「行程页 Tab 切换」「4 个高频入口」，**有一半以上是空的**。

### 3.3 🟠 数据底座太薄，撑不起定位

- **城市 4 个**（北京/上海/成都/杭州），**景点 16 条**（每城 4 条）—— 全是种子数据
- `city_galleries`、`city_tags` 两张表 **0 行**，且无任何代码写入（已下线功能的残骸）
- 景点**没有坐标** → 天气只能到城市级；`trip_items.ref_poi_id` 是**死引用**（`poi-*` 与 `city_spots.id` 的 `cs_*` 对不上）

**影响**：`chat.js:30` 会把「合作城市的真实景点」注入 prompt，但只有 16 条。
用户问「去新疆」，注入内容命中不了，AI 全靠自己的知识自由发挥 →
**价格、时长、开放时间的可靠性会明显下降，而界面上这些数字看起来却很权威。**

### 3.4 🟠 用户旅程在「生成完行程」就断了

```
灵感 ──✅── 规划 ──✅── 编辑 ──✅── [预订 ❌] ── [出行中 ❌] ── [回来后 ❌]
```

- 没有「出行中」：不能打卡、不能记录实际花费、不能临时加项
- 没有「回来后」：没有回顾、没有分享、没有真实足迹沉淀（`footprints` 是 mock）
- **影响**：用户拿到行程后，下一次打开 App 就是出发那天。**留存没有钩子。**

### 3.5 🟡 README 已严重滞后于代码

| README 说 | 实际 |
|---|---|
| `README.md:61` 品牌色 `#0F7BE0` | `tokens.css:25` 已是 `#1677ff`（Ant Design 蓝） |
| `README.md:18` 首页「城市定位」 | 城市切换已下线，改成 4 城轮播 |
| `README.md:30` 输入条有「语音 / 图片 / 联网 / 深度思考开关」 | `Chat.vue:118-128` 只有一个文本框 + 发送按钮 |
| `README.md:37` 底部有「一键预订」 | 已下线，改为纯预算条 `Trip.vue:172-175` |
| `README.md:62` 强调层含 teal `#0E7C6B` | `tokens.css` 里没有 teal，金额已统一红色 `--c-money` |

### 3.6 🟡 消息中心是"演示装置"

`messages` 表 6 条（trade 2 / trip 2 / social 1 / system 1），**全部是种子数据**，
没有任何业务代码在生成消息。也就是说「去支付」「查看行程」这些落点背后的场景从未真实发生过。

---

## 四、建议

### 建议 1（最高优先）：把定位收窄成一句话，然后删掉不属于它的东西

**收窄为：一句话生成可编辑行程的 AI 行前规划工具。**

具体动作 —— 这些都是删减，不是新增：

1. 首页卡片去掉「X 人报名」（`Home.vue:124`）—— 没有交易系统，报名数就是假的
2. 行程列表「X 项待预订 / 去预订」→ 改成「X 项安排 / 查看」（`TripList.vue:47-50`）
3. 消息中心「去支付」落点改成「查看行程」，或直接下线这类消息（`Message.vue:49`）
4. 「我的订单」入口先摘掉（`profile.vue:86-92`）—— **宁可少三个入口，也不要一个点不动的入口**
5. `profile.vue` 的「已解锁 N 城」改成真数据，或先换成不含数字的文案

> 判断标准很简单：**界面上每一句话，都要能对应到一条真实的后端能力。**

### 建议 2：与其补交易，不如把 AI 能力的"可信度"做厚

用户选择 AI 工具，第一诉求是「别骗我」。当前最该补的是：

| 做什么 | 为什么 |
|---|---|
| 返程具体站名（已规划的第 2 步） | 提升行程可执行性，成本低 |
| 价格标注来源与置信度，写清「参考价，以现场为准」 | 现在是"看起来权威的估算值"，最容易翻车 |
| 景点库扩到 20–30 城 + **补坐标** | 一举两得：AI 少胡说 + 天气精度提升 |
| 「引用来源」要么接真数据，要么**删掉** | 现在是永假，比没有更糟 |

### 建议 3：把闭环从「生成」延伸到「出行中 / 回来后」

按性价比排序：

1. **让 FAB「新增安排」真的能用**（`Trip.vue:151`）—— 用户手动加项是高频真需求，比"预订"更该做
2. **出行中**：当天行程项可打勾/打卡（数据结构已经够用，`trip_items.locked` 字段现成的）
3. **回来后**：把 `footprints` 变成真实统计（从已完成的 trips 反算城市/天数），
   让「已解锁 N 城」从假变真 —— 这一步能同时修掉 2.3 和 3.4 两个问题

### 建议 4：商业化路径（三种，按"与现有架构的贴合度"排序）

| 路径 | 做法 | 成本 | 贴合度 |
|---|---|---|---|
| **A. 跳转分销** | 「去预订」改为跳携程/飞猪联盟链接，行情是联盟佣金 | 低 | 高（UI 都现成了） |
| **B. 会员制** | 免费 3 条行程，会员解锁更多目的地 / 更长天数 / 不限次 AI 补排 | 中 | 高（限流器已存在，`chat.js:12`） |
| **C. 自建交易** | 酒店/门票 API 直连 | 高 | 低（不建议，重资产） |

**我倾向 A 或 B。** C 会把一个"工具"变成一家"旅行社"，那是完全不同的生意。

### 建议 5：工程债

- `unread-count` 接口调用起来（`message.js:42` 已实现，前端未用 → 切分类后未读数会错）
- 死代码/死字段清理：`citation`、`city_galleries`、`city_tags`、`ref_poi_id`
- 未实现的 UI 统一用特性开关控制，别再留"点了没反应"的按钮
- README 更新到与代码一致（上面的表格直接可用）

---

## 五、需要你拍板的三个选择

1. **定位取舍**：认下「AI 行前规划工具」，把 OTA / 社区的外壳剥掉？
   （还是你原本就打算往后做交易和内容社区 —— 那我建议先补哪个，别两头都留一半）
2. **下一步动手做什么**：补齐 AI 可信度（扩景点库 + 站名）／做「出行中打卡」闭环 ／做商业化跳转？
3. **「我的」页面怎么处理**：先修成真数据，还是先摘掉假数字？

---

*本报告只读盘点，未修改任何代码与数据。真库快照：12 张表，trips 8 条，trip_items 46 条，trip_hops 24 条。*
