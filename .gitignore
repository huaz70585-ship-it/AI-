# 依赖 / 构建产物
node_modules/
dist/

# 根目录孤儿 package-lock（无对应 package.json，误装产物）
/package-lock.json

# 环境变量（含 API Key，绝不入库）
.env
*.env
.env.local
!.env.example

# 本地数据库
backend/data/

# 工具与日志
.workbuddy/
*.log
.DS_Store