# 复制为 .env.local 后填写真实值：cp .env.example .env.local
# （.env.local 已被 .gitignore 的 *.local 规则忽略，不会进仓库）

# ── 腾讯位置服务（lbs.qq.com）────────────────────────────────
# 行程页「地图」Tab 的底图 Key，由 TripMap.vue 读取。
# Key 类型选「JavaScript API GL」，并给 Key 配 Referer 白名单。
#
# 未配置时：地图 Tab 显示「地图未启用」引导卡，其余功能完全不受影响。
VITE_TMAP_KEY=

# ⚠️ 改完这个文件必须重启 dev server —— Vite 只在启动时读取 .env 文件，HMR 不生效。
# ⚠️ VITE_ 前缀的变量会被编译进前端产物，明文可见。生产环境务必配 Referer 白名单，
#    或改为后端代理转发（Key 不出前端）。
